# Audit: Оставшиеся работы

## Выполнено

### P0 — Безопасность и баги
- ✅ Дыра в доступе к событиям — `update_event()` и `delete_event()` теперь проверяют `require_tree_access()`
- ✅ N+1 запросы — batch-загрузка в `event_service`, `relation_service`, `album_service`, `document_service`, `tree_service`
- ✅ Null-dereference — исправлен в `upload_event_media()`

### P1 — Чистка кода
- ✅ Удалены 5 мёртвых файлов: `Button.tsx`, `Input.tsx`, `Select.tsx`, `Card.tsx`, `HumanCard.tsx`
- ✅ `getRelationLabel()` вынесен в `src/lib/relations.ts`
- ✅ Геокодинг вынесен в `src/lib/geocode.ts`
- ✅ Убрана дублирующая загрузка адресов (лишний `useEffect`)
- ✅ Удалены неиспользуемые импорты (`import time` × 3, `HumanInfoResponse`)

---

## Осталось — сделать когда будет время

### P1 — Доделать
- [ ] **`get_db()`** скопирован в 9 API-файлов. Вынести в `dependencies.py` и импортировать оттуда
- [ ] **`toDateInput()`** дублируется 2 раза. Вынести в `lib/utils.ts`
- [ ] **`permission_service.py`** — весь класс нигде не вызывается. Удалить
- [ ] **Модели `Permission` и `RolePermission`** в `role.py` — не используются. Удалить

### P1 — Индексы в БД
Добавить в `docs/ddl.sql` и `seed_defaults()`:
- [ ] `CREATE INDEX idx_humans_tree_deleted ON humans(tree_id, is_deleted)`
- [ ] `CREATE INDEX idx_human_rel_from ON human_relationship(from_human_id)`
- [ ] `CREATE INDEX idx_human_rel_to ON human_relationship(to_human_id)`
- [ ] `CREATE INDEX idx_human_events_human ON human_events(human_id)`
- [ ] `CREATE INDEX idx_human_events_event ON human_events(event_id)`
- [ ] `CREATE INDEX idx_human_addresses_human ON human_addresses(human_id)`
- [ ] `CREATE INDEX idx_albums_human ON albums(human_id)`
- [ ] `CREATE INDEX idx_documents_human ON documents(human_id)`
- [ ] `CREATE INDEX idx_human_media_human ON human_media(human_id)`
- [ ] `CREATE INDEX idx_event_media_event ON event_media(event_id)`
- [ ] `CREATE INDEX idx_document_media_document ON document_media(document_id)`
- [ ] `CREATE INDEX idx_album_media_album ON album_media(album_id)`

### P2 — Docker
- [ ] **Multi-stage Dockerfile** для backend: убрать `--reload`, добавить non-root user
- [ ] **Multi-stage Dockerfile** для frontend: `npm run build` → `npm run start`, non-root user
- [ ] **docker-compose.yml**: убрать порт PostgreSQL 5432 наружу (оставить только internal)
- [ ] **docker-compose.yml**: source code volume mounts вынести в `docker-compose.dev.yml`

### P2 — Безопасность
- [ ] **CORS из env**: вынести список origins в `CORS_ORIGINS` env-переменную
- [ ] **JWT_SECRET**: убрать из `docker-compose.yml`, использовать `.env` файл
- [ ] **Лимит загрузки файлов**: добавить проверку `MAX_UPLOAD_SIZE` во все upload-эндпоинты
- [ ] **Валидация**: пароль ≥ 6 символов, пол "male"/"female", валидные даты, max_length имени дерева

### P2 — Inline схемы
- [ ] Вынести Pydantic-схемы из `relations.py`, `albums.py`, `documents.py` в `app/schemas/`

### P3 — Полировка
- [ ] Заменить `location.reload()` / `prompt()` / `confirm()` / `alert()` на кастомные диалоги
- [ ] Вынести `FilterPanel` из render-функции (создаётся каждый рендер)
- [ ] Убрать 36 использований `any` во фронтенде
- [ ] Унифицировать `TreeInfo` (определён 3 раза: TreeContext, types/index)
- [ ] Добавить `reactStrictMode: true` и `poweredByHeader: false` в `next.config.js`

---

## Порядок выполнения

Когда будет время, выполнять в этом порядке:
1. Индексы в БД (быстро, сразу выигрыш в производительности)
2. `get_db()` централизация (5 минут)
3. Docker multi-stage (средне, но важно для деплоя)
4. CORS/JWT из env (средне, важно для безопасности)
5. Валидация входных данных (средне)
6. Остальное — по мере необходимости
